---
description: Re-index the project into Qdrant
---
Trigger a full semantic re-indexing of the project using the `reindex` tool with `full: true`. Then check the status with `index_status` and report the results.
